"""Aegis Python client subpackage.

Python services are always server-side. There is no client-side (browser)
equivalent for Aegis Python. For browser authentication in the React frontend,
use ``@elastic-resume-base/aegis/client`` (TypeScript).
"""
