/**
 * Re-exports `correlationIdHook` from `@elastic-resume-base/toolbox`.
 * Kept here to preserve the original import path used throughout the service.
 */
export { correlationIdHook } from '@elastic-resume-base/toolbox';

